[project]
name = "aeso-load-forecasting"
version = "0.1.0"
description = "AESO-style Alberta load forecasting and market analytics MVP."
requires-python = ">=3.10,<3.13"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
